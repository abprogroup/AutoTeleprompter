# AI Protocol: AutoTeleprompter

To protect the project, all AI agents (Claude, Antigravity) must:

1.  **NO BACKUP LOOPS**: Never track `backups/` in Git.
2.  **RELEASE PATH**: Move stable v1/v2 code to `releases/`.
3.  **FULL NAME**: Always use "AutoTeleprompter".
4.  **DAILY LOG**: Update `DAILY_LOG.md` at end of session.
