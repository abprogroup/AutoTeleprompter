# AI Protocol: AutoTeleprompter

To protect the project, all AI agents (Claude, Antigravity) must:

1.  **NO BACKUP LOOPS**: Never track `backups/` in Git.
2.  **RELEASE PATH**: Move compiled stable artifacts (v1, v2, v3) to `releases/` subdirectories.
3.  **COMPILED ONLY**: Strictly forbid storing source code inside the `releases/` folder.
4.  **FULL NAME**: Always use "AutoTeleprompter".
5.  **DAILY LOG**: Update `DAILY_LOG.md` at end of session.
